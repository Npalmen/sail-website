/** Pure debossed SAIL mark — same hue as canvas, relief via highlight/shadow only. */
export function SailBrandMark() {
  return (
    <div aria-hidden="true" className="sail-brand-mark">
      SAIL
    </div>
  );
}
